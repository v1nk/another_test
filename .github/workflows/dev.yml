# This is a basic workflow to help you get started with Actions
name: DEV build

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  push:
    branches: [ stage, dev ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Show Branch Name
        shell: bash
        run: |
          echo "${{ steps.extract_branch.outputs.branch }}"
          echo "${{ github.ref_name }}"
          echo "echo - ${{ github.job }}"
          echo "job id - ${{ github.action }}"

#      - name: Deploy branch to remote
#        shell: bash
#        run: |
#          composer self-update --no-interaction && composer install --no-interaction --optimize-autoloader
#          vendor/bin/blt -vvv artifact:deploy --commit-msg "GitHub build - $BITBUCKET_BUILD_NUMBER, Commit $BITBUCKET_COMMIT" --branch "${{ github.ref_name }}" --no-interaction
