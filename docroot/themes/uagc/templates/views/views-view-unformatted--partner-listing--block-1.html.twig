<div class="partner-display__listing-content">
  {{ title_prefix }}
  {{ title }}
  {{ title_suffix }}
  {% if header %}
    <header>
      {{ header }}
    </header>
  {% endif %}

    {{ exposed }}
    {{ attachment_before }}

    {% if rows -%}
            {{ rows }}
    {% elseif empty -%}
    <div>No partners found.</div>
        {{ empty }}
    {% endif %}

</div>