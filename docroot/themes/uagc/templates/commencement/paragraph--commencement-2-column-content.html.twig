{#
/**
 * @file
 * Default theme implementation to display a paragraph.
 *
 * Available variables:
 * - paragraph: Full paragraph entity.
 *   - id: The paragraph ID.
 *   - bundle: The type of the paragraph, for example, "image" or "text".
 *   - authorid: The user ID of the paragraph author.
 *   - createdtime: Formatted creation date. Preprocess functions can
 *     reformat it by calling format_date() with the desired parameters on
 *     $variables['paragraph']->getCreatedTime().
 * - content: All paragraph items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - paragraphs: The current template type (also known as a "theming hook").
 *   - paragraphs--type-[type]: The current paragraphs type. For example, if the paragraph is an
 *     "Image" it would result in "paragraphs--type--image". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - paragraphs--view-mode--[view_mode]: The View Mode of the paragraph; for example, a
 *     preview would result in: "paragraphs--view-mode--preview", and
 *     default: "paragraphs--view-mode--default".
 * - view_mode: View mode; for example, "preview" or "full".
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @see template_preprocess_paragraph()
 *
 * @ingroup themeable
 */
#}
{%
set classes = [
'section',
'section--type-' ~ paragraph.bundle|clean_class,
view_mode ? 'section--display-mode-' ~ view_mode|clean_class,
'large-image-copy-block',
'commencement-2-column-content',
]
%}
    {% if (content.field_large_image_on_left.0['#markup'] == "Off") %}
    {% set classes = classes|merge(['image-on-right']) %}
    <div{{ attributes.addClass(classes) }}>
        <div class="large-image-copy-block--content-wrapper">
            <h3 class="large-image-copy-block--header">{{content['field_large_image_header']}}</h3>
            <div class="large-image-copy-block--content">{{content['field_large_image_content']}}</div>
            {% if (content.field_large_image_link_url|keys|length == 2)  %}
                {# <div class="large-image-copy-block--link"><a href="/request-information">Request More Info</a></div>
                <br> #}
            {% else %}
                <div class="large-image-copy-block--link">{{content['field_large_image_link_url']}}</div>
            {% endif %}
        </div>
        <div class="large-image-copy-block--image-wrapper">
            <div class="large-image-copy-block--image">
                {% if(content.field_large_image_youtube_url|keys|length == 2) %}
                    {{content['field_large_image']}}
                {% else %}
                    <div class="large-image-copy-block--iframe-wrapper">
                        {% set video_url = content.field_large_image_youtube_url|render|striptags|trim %}
                        <iframe  class="large-image-copy-block--iframe" src="https://www.youtube.com/embed/{{video_url}}?rel=0&wmode=transparent" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
                    </div>
                {% endif %}
            </div>
        </div>
    {% else %}
        <div{{ attributes.addClass(classes) }}>
            <div class="large-image-copy-block--image-wrapper">
                <div class="large-image-copy-block--image">
                    {% if(content.field_large_image_youtube_url|keys|length == 2) %}
                        {{content['field_large_image']}}
                    {% else %}
                        <div class="large-image-copy-block--iframe-wrapper">
                            {% set video_url = content.field_large_image_youtube_url|render|striptags|trim %}
                            <iframe class="large-image-copy-block--iframe" src="https://www.youtube.com/embed/{{video_url}}?rel=0&wmode=transparent" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
                        </div>
                    {% endif %}
                </div>
            </div>
            <div class="large-image-copy-block--content-wrapper">
                <h3 class="large-image-copy-block--header">{{content['field_large_image_header']}}</h3>
                <div class="large-image-copy-block--content">{{content['field_large_image_content']}}</div>
                {% if (content.field_large_image_link_url|keys|length == 2)  %}
                    {# <div class="large-image-copy-block--link"><a href="/request-information">Request More Info</a></div>
                    <br> #}
                {% else %}
                    <div class="large-image-copy-block--link">{{content['field_large_image_link_url']}}</div>
                {% endif %}
            </div>
    {% endif %}
    
</div>
